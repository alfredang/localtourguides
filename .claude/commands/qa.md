---
description: Run the full end-to-end QA pass on the LocalTourGuides site
---

Launch the `site-qa` agent to run its full standard pass against the running site (start `npm run dev` first if nothing is listening on ports 4000/5173). Relay its pass/fail report back verbatim, most severe findings first, then propose fixes for any failures — but don't apply them without confirmation.

$ARGUMENTS
